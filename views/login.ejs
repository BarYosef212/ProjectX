<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/Styles/main.css" />
  </head>
  <body>
    <%- include('partials/header') %>
    <main style="width:15%; margin:0 auto;text-align: center;">
      <h2 class="login-title">Log In</h2>
      <form
        class="user-form"
        id="user-form"
        action="/login"
        method="POST"
      >
        <div class="log-input-box">
          <label for="user-name">Email:</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Enter your Email"
            required
          />
        </div>
       
        <div class="log-input-box">
          <label for="password">Password:</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Enter your password"
            required
          />
        </div>

        <% if (typeof error !== 'undefined') { %>
          <p style="color: red; text-align: left"><%= error %></p>
          <% } %>
          <% if (typeof message !== 'undefined') { %>
            <p class="success-message" style="color: green; text-align: left"><%= message %></p>
            <script>
              // Redirect to the homepage after 4 seconds
              setTimeout(function() {
                window.location.href = '/';
              }, 1300);
            </script>
          <% } %>
        <button class="btn btn-login" type="submit">Log in</button>
        <p class="sign-option">
          Don't have an account?
          <a class="main-link" href="/register">Sign up</a>
        </p>
      </form>
    </main>
  </body>
</html>
